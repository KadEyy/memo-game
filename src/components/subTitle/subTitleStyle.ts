import styled from 'styled-components';

export const SubTitleCore = styled.div`
    width: 100%;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;

export const SubTitleParagraph = styled.p`
    width: fit-content;
    font-size: 1.6em;
    padding: 0 10px;
    margin: 0;
`;